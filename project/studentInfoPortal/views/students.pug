doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
    h1 Students list 
    ul 
       each student in students
        li
        a(href=`/student/${student.id}`) #{student.name} - #{student.course}

               a(href="/")  Back to Home
